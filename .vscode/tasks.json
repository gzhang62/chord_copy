{
    "tasks": [
        {
            "type": "shell",
            "label": "Make a4",
            "command": "make", 
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Run make and build the project."
        },
    ],
    "version": "2.0.0"
}